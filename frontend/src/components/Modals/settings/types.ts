// Types partagés entre les sections des paramètres (SettingsModal).
// ResourceType est utilisé à la fois par ResourceSection et par le
// composant principal SettingsModal (toggleResource, etc.).
export type ResourceType = "extensions" | "skills" | "prompts" | "themes";
